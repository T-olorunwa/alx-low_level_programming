#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - Prints the length of a string
 * @s: The string length to be counted
 *
 * Return: The length of string to output
 */

int _strlen_recursion(char *s)
{
	int leng = strlen(s);

	return (leng);
}
