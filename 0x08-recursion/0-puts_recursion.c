#include <stdio.h>
#include "main.h"
#include "string.h"

/**
 * _puts_recursion - prints a string with
 *			a new line
 * @s: The string to be printed
 */

void _puts_recursion(char *s)
{
	puts(s);
}
