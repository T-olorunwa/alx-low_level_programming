#include <stdio.h>

/**
 *  main - Entry point
 *
 *  Return: Always 0 (Success)
 */

int main(void)
{
	char alpha()
	{
		int x;

		for (x = 'a'; x <= 'z'; x++)
			putchar(x);
			putchar('\n')
	}

	return alpha();
	return (0);
}
